# Learning

- `learning_level`: `delegated`

集中 Review 时沿 `save → admission locks → Coordinator/source row locks → Contract binding → main/link inserts → integrity read` 路线检查。Owner Review/Learning 延后至 demo milestone。
