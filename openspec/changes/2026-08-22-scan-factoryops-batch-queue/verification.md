# Verification：扫描 FactoryOps 批次队列

- `status`: `design-only`

## 设计阶段基线

- `python -m unittest discover -s frontend -p 'test_*.py' -v`：8 passed。

## 待实施验证

真实命令、结果、失败限制、浏览器证据和 dataset diff 将在实施后记录，不预先声明通过。
