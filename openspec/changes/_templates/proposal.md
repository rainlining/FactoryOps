# Change 提案：<change-id>

> `<change-id>` 必须使用 `YYYY-MM-DD-修改内容`，例如 `2026-08-10-define-vision-inspection-contract`。

## 元数据

- `change_id`: `<change-id>`
- `status`: `proposed`
- `learning_level`: `deep | standard | delegated`
- `first_deep_reference`: `N/A | <change-id>`
- `depends_on`: `[]`
- `spec_refs`: `[]`

## 为什么要做

说明当前问题、受影响对象，以及为什么需要独立 Change。

## 范围

- 本 Change 唯一核心能力：
- 包含：

## 非目标

- 明确本 Change 不做什么。

## 预期影响

- 新增或修改的规格：
- 预期涉及的代码或文档区域：
- 外部 Contract 影响：

## 依赖与顺序

- 前置 Change：
- 后续被阻塞的 Change：

## 学习等级理由

说明为何是 deep、standard 或 delegated。若采用重复模式等级递减，引用首次 deep Change，并分别说明相同语义和新增语义。

## 验收摘要

- 技术验收：
- 学习验收：
