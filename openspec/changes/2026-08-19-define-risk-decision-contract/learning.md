# 学习计划

- `learning_level`: `deep`
- `gate_status`: `awaiting-learning-gate`

Review 需解释 Recommendation→Risk binding、STOP_LINE approval invariant 和为何 Risk 不执行动作。Owner 修改：为 `HOLD_BATCH` 增加一个合法 ALLOW fixture。Failure exercise：把 STOP_LINE 改为 ALLOW 且 approval_required=false，观察稳定拒绝。
