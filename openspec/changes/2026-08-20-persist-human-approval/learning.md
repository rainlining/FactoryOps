# Learning

- `learning_level`: `deep`
- `gate_status`: `deferred-until-demo-milestone`
- Walkthrough：save → admission locks → Risk/Fusion locks → Contract relation → current/history transaction。
- Owner 修改：增加一种 REJECTED reason code 的持久化测试。
- Failure exercise：并发提交 APPROVED/REJECTED，观察只保留一个 revision 2 winner。
