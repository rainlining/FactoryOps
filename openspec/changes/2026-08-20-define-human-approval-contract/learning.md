# Learning

- `learning_level`: `deep`
- `gate_status`: `deferred-until-demo-milestone`
- Walkthrough：Risk source binding → approval key → schema/preflight → revision relation。
- Owner 修改：增加一条合法 REJECTED fixture，并解释它为何不等于业务动作已执行。
- Failure exercise：把 APPROVED actor 改为 SYSTEM，观察稳定拒绝；恢复后测试通过。
