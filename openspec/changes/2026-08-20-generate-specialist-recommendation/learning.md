# Learning

- `learning_level`: `deep`
- Walkthrough：`generate → 完整 Contract reader → provider provenance/replay admission → provider boundary → draft reference authorization → deterministic Contract build → persistence snapshot fencing`。
- Owner 修改任务：集中 Learning Gate 时为 recorded provider 增加一个合法 quality draft 配置并证明 identity 仍由应用层控制。
- Failure/debug exercise：让 provider 阻塞，在此期间完成/失败 parent Execution，再释放 provider；观察保存被拒绝且 Recommendation 不落库。
- 当前状态：Owner Review/Learning 延后至成品可展示后。
