# 学习计划

- `learning_level`: `deep`
- `gate_status`: `deferred-until-demo-milestone`

集中 Review 时沿 `Fusion input refs → validation → deterministic ordering → canonical relation` 路线检查，并完成跨 Run 冲突故障实验。Owner 修改和 Learning Gate 延后。
